const request = require('request');
const mongoose = require('mongoose');

function myFunc() {
  console.log('ya did it ya');
};

//this file will have to handle the writing to the mongodb database, or array while starting out
